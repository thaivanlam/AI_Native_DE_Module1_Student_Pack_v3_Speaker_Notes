from .config import SETTINGS


def fetch_all_pages(resource, updated_after=None, page_size=100, timeout=10, max_retries=3):
    """TODO Buổi 6: requests + API key + timeout + pagination + retry."""
    raise NotImplementedError('Implement fetch_all_pages()')
